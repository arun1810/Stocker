<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"


    android:layout_width="match_parent"
    android:layout_height="@dimen/sort_layout_height"
    android:elevation="5dp"

    android:background="?android:colorPrimaryDark"

    >

    <com.example.stocker.view.customviews.SortImageButton
        android:id="@+id/name_sort"
        android:layout_width="0dp"
        android:src="@drawable/ic_sort_alphabetical_ascending"
        android:scaleType="centerInside"
        android:tint="?android:textColorPrimary"
        android:layout_height="22dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/date_sort"

        />
    <com.example.stocker.view.customviews.SortImageButton
        android:id="@+id/date_sort"
        android:layout_width="0dp"
        android:layout_height="22dp"
        android:src="@drawable/ic_sort_clock_ascending_outline"
        android:scaleType="centerInside"
        android:tint="?android:textColorPrimary"

        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/name_sort"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        />
</androidx.constraintlayout.widget.ConstraintLayout>